<form method="post" action="{{ path('app_delete_permis2', {'id_permis': permi.idPermis}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ permi.idPermis) }}">
    <button class="btn">Delete</button>
</form>

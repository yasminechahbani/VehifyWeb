<form method="post" action="{{ path('app_equipement_delete', {'idEquipement': equipement.idEquipement}) }}" onsubmit="return confirm('Are you sure you want to delete this equipment?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipement.idEquipement) }}">
    <button type="submit" class="dropdown-item text-danger">
        <i class="fas fa-trash-alt me-2"></i>Delete Equipment
    </button>
</form>
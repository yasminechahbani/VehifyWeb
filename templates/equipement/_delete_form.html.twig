<form method="post" action="{{ path('app_equipement_delete', {'idEquipement': equipement.idEquipement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipement.idEquipement) }}">
    <button class="btn">Delete</button>
</form>

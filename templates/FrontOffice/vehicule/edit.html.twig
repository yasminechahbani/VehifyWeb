{% extends 'FrontOffice/base.html.twig' %}
{% set active_page = 'vehicules' %}

{% block title %}Modifier le véhicule{% endblock %}

{% block content %}
<!-- Header avec image -->
<div class="container-fluid page-header mb-5 p-0" style="background-image: url({{ asset('img/carousel-bg-1.jpg') }});">
    <div class="container-fluid page-header-inner py-5">
        <div class="container text-center">
            <h1 class="display-3 text-white mb-3 animated slideInDown">Modifier le véhicule</h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb justify-content-center text-uppercase">
                    <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Accueil</a></li>
                    <li class="breadcrumb-item"><a href="{{ path('app_vehicule_index') }}">Véhicules</a></li>
                    <li class="breadcrumb-item text-white active" aria-current="page">Édition</li>
                </ol>
            </nav>
        </div>
    </div>
</div>

<!-- Formulaire de modification -->
<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow rounded-4 p-4">
                <h2 class="mb-4 text-center text-primary">Formulaire de modification</h2>

                {{ include('FrontOffice/vehicule/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <div class="mt-4 d-flex justify-content-between">
                    <a href="{{ path('app_vehicule_index') }}" class="btn btn-outline-primary px-4 py-2 rounded-pill shadow-sm">
                        <i class="fas fa-arrow-left me-2"></i> Retour à la liste
                    </a>

                    {{ include('FrontOffice/vehicule/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
